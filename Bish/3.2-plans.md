# Bish3.2 plans

* [x] 🔴 让程序能获取自身的源代码
  - 需要在编译产物中储存字节码对应的行数和源代码位置
* [x] 🔴 将parser解析产物作为`BishObject`
* [ ] 🟢 异步文件操作库
* [ ] 🟡 网络库
* [x] 🟡 JSON库
* [ ] 🔴 加入内插字符串
* [x] 🟡 完善对于各种C#层异常的包装
  - 用源生成器处理？或者用Rider插件检查？
  - [x] `CompileService`中的文件操作
  - [x] `thread`库的线程操作
* [x] 🟢 用Bish实现一部分标准库
  - [x] 需要增加更简单的扩展类的语法
  - [x] 需要让`import`可以省略后缀名
  - 比如`Iterator`/`AsyncIterator`相关
* [x] 🟡 让`del`能删到上层作用域的变量
  - [x] 统一化`get`/`set`/`del`的查找逻辑
* [x] 🔴 合并`hook_create`和`hook_init`
  - 去掉`bool`和`int`的奇怪处理
* [x] 🟢 加入元类（主要用来给类型本身增加方法）
* [ ] 🔴 加入`defer`，保证在函数提前返回时也能运行
  - 可能需要在`scope`上增加`defer`列表，在合适时机调用
  - 将`with`作为其包装，并且加入不缩进的形式
* [ ] 🟡 加入类似*python*中`settrace`的机制
  - 把内部目前的3种handler（`try`, `yield`, `await`）统一成某种`effect`？
* [ ] 🟢 让`object`的解构赋值/模式匹配支持键值
